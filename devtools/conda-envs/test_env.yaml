name: test

channels:
  - conda-forge

dependencies:

    # Base depends
  - python<=3.11
  - pip
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - tensorflow<=2.15
    - tensorflow-probability<=0.23
    - geometric-algebra-attention
