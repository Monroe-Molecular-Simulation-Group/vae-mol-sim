name: test

channels:
  - conda-forge

dependencies:

    # Base depends
  - python<=3.10
  - pip
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - tensorflow
    - tensorflow-probability
    - geometric-algebra-attention
